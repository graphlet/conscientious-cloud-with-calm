Go to https://calm.finos.org/working-with-calm/installation/ to read and follow the installation instructions.

Verify CALM installation:

```bash
calm --version
```

Enable AI Assistance for Copilot Chatmode:

```bash
calm copilot-chatmode -d .
```

Create a basic structure for your architecture files:

```bash
mkdir architectures
```
